import React from "react";
import { Header, Image, Button } from "semantic-ui-react";

import "./style.css";

const AboutSection = () => {
    return (
        <div>

        </div>
    );
};

export default AboutSection;